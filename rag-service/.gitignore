# Python bytecode
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
.venv/
venv/
env/

# Build / packaging
build/
dist/
*.egg-info/

# Test / lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# uv.lock IS committed. CI resolves dependencies on every run, so without a
# lockfile the build is not reproducible and a transitive release can turn it
# red with no code change. The "single uv workspace" this was waiting on never
# materialised, and there is still exactly one Python service.

# Editors
.idea/
.vscode/
